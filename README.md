# Gemini Chat Ghost

Chrome extension for `gemini.google.com` that adds a one-click delete action by renaming chats to `deleted`, with an optional setting to hide those chats from the sidebar.

## Install

1. Open `chrome://extensions`.
2. Enable **Developer mode**.
3. Click **Load unpacked**.
4. Select this folder.
5. Open Gemini and refresh the tab once.
